import { NavigationState, PartialState } from '@react-navigation/routers/lib/typescript/src/types';
/**
 * Base router object that can be used when writing custom routers.
 * This provides few helper methods to handle common actions such as `RESET`.
 */
declare const BaseRouter: {
    getStateForAction<State extends NavigationState>(state: State, action: import("@react-navigation/routers/lib/typescript/src/CommonActions").Action): State | PartialState<State> | null;
    shouldActionChangeFocus(action: import("@react-navigation/routers/lib/typescript/src/CommonActions").Action): boolean;
};
export default BaseRouter;
